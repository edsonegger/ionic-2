atualizar o nodeJs
sudo npm cache clean -f
sudo npm install -g n
sudo n stable

sudo ln -sf /usr/local/n/versions/node/<VERSION>/bin/node /usr/bin/node

GitHub via ssh com keygen
ssh-keygen -t rsa -C "edson.egger@gmail.com"
ssh-add id_rsa
-- no github precisa add a chave publica

-- para testar o login
ssh -T gh


--- criar uma nova app no Ionic
ionic start <nomeApp> <template> --v2 --ts
                      black       forçar v2     typescript
                      tutorial
                      tabs

-- start
ionic serve



--- git hub
git init para inicializar o repositorio

git status

git add .   -- add todos arquivos para fazer commit

git commit -m <descrição entre "">
git commit -m "inicial"

-- identidade para os commits
git config --global user.name "Edson Edgar Egger"
git config --global user.email "edson.egger@gmail.com"

-- add o repositorio remoto
git remote add origin git@github.com:edsonegger/ionic-2.git
				-- alias para o repositorio 

-- listar as configurações
git config --list --local

-- enviando para o github
git push origin master

-- pegando do github
git pull origin master